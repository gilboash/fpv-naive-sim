{
  "name": "fpvsim",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5180",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "check:flight": "node --experimental-strip-types tools/flight-check.ts",
    "check:browser": "node tools/browser-check.mjs",
    "replay": "node --experimental-strip-types tools/replay.ts",
    "identify": "node --experimental-strip-types tools/identify.ts",
    "reference": "node --experimental-strip-types tools/optimal-lap.ts"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  }
}
